import { useEffect } from "react";
import { useNavigate } from "react-router-dom";
import GoogleButton from "react-google-button";

const GoogleLoginButton = () => {
    return <GoogleButton />;
}

export default GooleLoginButton;